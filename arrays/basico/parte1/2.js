//Crie um array chamado numeros com cinco números. Mostre no console o primeiro e o último número do array.

let numeros = [10, 25, 37, 42, 58];

// Exibe o primeiro número (posição 0)
console.log("Primeiro número: ", numeros[0]);

// Exibe o último número (posição length - 1)
console.log("Último número: ", numeros[numeros.length - 1]);

console.log("Array completo: ", numeros);