//Crie um array chamado frutas e coloque dentro três frutas de sua preferência. Depois, exiba o array no console.

let frutas = ["Maçã", "Banana", "Uva"];
console.log(frutas);

for (let i = 0; i < frutas.length; i++) {
  console.log(frutas[i]);
}